import ReactDOM from 'react-dom';
import React from 'react';
import Hero from '../js/parts/Hero'

function App() {
	return <div>
	
		<Hero />
		</div>
} 
ReactDOM.render(<App />, document.getElementById('root'))
